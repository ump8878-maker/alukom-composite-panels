node_modules/
.next/
out/
build/
.turbo/
.vercel/
.env*.local
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo

# Исходники PNG из Higgsfield (~30 МБ каждый) — не нужны в bundle/git.
# Финальные WebP лежат в public/images/ — они и попадают в repo.
photo/
.vercel
